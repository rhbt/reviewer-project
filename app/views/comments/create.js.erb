$(".comment-container").append("<%= escape_javascript(render('new_comment')) %>");
  
for (i = 1; i < 6; i++) {
	$("#" + i + "-star").attr("class", "star-empty");
}

$("input:checked").removeAttr("checked");

$("#new_comment textarea").val("");
